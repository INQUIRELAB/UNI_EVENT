// Polarity palette — LOCKED across the library, the schema, and the Labs.
// Warm = ON (a brightness increase), cool = OFF (a decrease).
export const PALETTE = {
  on: "#FF5A3C", // warm — ON / p=1
  off: "#3CC8FF", // cool — OFF / p=0
  bg: "#05060A", // near-black space
  fg: "#cdd3e0",
  muted: "#7d8597",
} as const;
